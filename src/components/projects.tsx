import { ProjectsCarousel } from "@/components/projects-carousel";

export function Projects() {
  return (
    <section
      id="projects"
      className="projects-section relative w-full min-w-0 scroll-mt-24"
    >
      <ProjectsCarousel />
    </section>
  );
}
